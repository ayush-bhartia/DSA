//**to find out the number of rows and colums in a matrix**

// numRows = sizeof(matrix)/sizeof(matrix[0]);
// numCols = sizeof(matrix[0])/sizeof(matrix[0][0]);

// **to sort in decreasing function using sort() function

// sort(vector.begin(),vector.end(),greater<int>())
// sort(do, do, here you can call a boolean function of your own condition as well)

// **